---
uid: RW001
category: 代码操作项
sub-category: 重写
title: 使用 CALCULATE 重写 TOTALxTD
author: Daniel Otykier
updated: 2025-02-10
---

代码操作 `RW001`（重写）：**使用 CALCULATE 重写 TOTALxTD**

## 描述

例如 [`TOTALMTD`](https://dax.guide/TOTALMTD)、[`TOTALQTD`](https://dax.guide/TOTALQTD) 和 [`TOTALYTD`](https://dax.guide/TOTALYTD) 等函数，都可以通过 [`CALCULATE`](https://dax.guide/CALCULATE) 函数来改写。

### 示例

将以下内容：

```dax
 TOTALYTD([Total Sales], 'Date'[Date])
```

改为：

```dax
CALCULATE([Total Sales], DATESYTD('Date'[Date]))
```

## 为什么 Tabular Editor 会这样建议？

当你需要添加额外筛选条件或修改计算上下文时，这种改写会很有用。

> [!NOTE]
> 此代码操作属于 **重写** 类别，这意味着它并不代表通用建议或最佳实践。 Instead, the code action provides a quick way to rewrite the code in a different way, for example as part of a larger refactoring. After applying the code action and until further edits are made, you may see an **Improvement** or **Readability** code action that suggests to modify the code back to its original state.