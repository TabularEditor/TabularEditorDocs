---
uid: DR005
category: 代码操作项
sub-category: 可读性
title: 为临时列添加前缀
author: Daniel Otykier
updated: 2025-01-09
---

代码操作 `DR005`（可读性）**为临时列添加前缀**

## 描述

It is recommended to use a consistent prefix for temporary columns, to more easily distinguish them from base columns or measures. 默认前缀为 `@`，但你可以在 **工具 > 偏好 > DAX编辑器 > 代码操作** 下配置要使用的前缀，以符合你偏好的风格。

### 示例

将以下内容：

```dax
ADDCOLUMNS(
    'Sales',
    "Sales With Tax", 'Sales'[Sales Amount] * 1.25
)
```

改为：

```dax
ADDCOLUMNS(
    'Sales',
    "@Sales With Tax", 'Sales'[Sales Amount] * 1.25
)
```

## 为什么 Tabular Editor 会这样建议？

此代码操作旨在提升 DAX 代码的可读性。 By using a consistent naming convention for your temporary columns, it is easier to understand the purpose of each column, and to distinguish between base columns, measures, and extension columns.

## 备注

此代码操作还提供一个 **（所有出现位置）** 变体，当代码中有多个片段可以改进时会显示。 This variant will apply the code action to all relevant sections of the document at once.

## 相关内容：

- [DR004 - 为变量添加前缀](xref:DR004)

## 延伸阅读：

- [SQLBI：在 DAX 中为临时列命名](https://www.sqlbi.com/articles/naming-temporary-columns-in-dax/)