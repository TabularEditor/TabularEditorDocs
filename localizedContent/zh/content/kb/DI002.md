---
uid: DI002
category: 代码操作项
sub-category: 改进
title: 删除未使用的变量
author: Daniel Otykier
updated: 2024-12-19
---

代码操作 `DI002`（改进） **删除所有未使用的 VAR 变量**

## 描述

在 `VAR` 变量块的 `RETURN` 部分未被使用（无论是直接使用，还是通过其他变量间接使用）的变量，都应删除。

## 示例

将以下内容：

```dax
VAR _internetSalesTaxed = [Internet Sales] * 1.25
VAR _cogs = [Cost of Sales]
VAR _cogsTaxed = _cogs * 1.25
RETURN _internetSalesTaxed
```

改为：

```dax
VAR _internetSalesTaxed = [Internet Sales] * 1.25
RETURN _internetSalesTaxed
```

## 为什么 Tabular Editor 会这样建议？

未使用的变量会让你的代码更难阅读和理解。 By removing them, you make your code more concise and easier to maintain.

未使用的变量也表明代码可能存在错误，或者该变量原本用于某些用途，但后来相关内容被移除了。 By removing the variable, you avoid potential confusion for other developers who may be reading your code.

如果某个 VAR 变量被其他 VAR 变量引用，但这些变量都没有在变量块的 `RETURN` 部分中使用，那么可以安全地将它们全部删除。

## 备注

此代码操作提供一个 **（所有出现位置）** 变体，当代码中有多个片段可以改进时会显示。 This variant will apply the code action to all relevant sections of the document at once.

## 相关内容：

- [DI001 - 删除未使用的 VAR 变量](xref:DI001)