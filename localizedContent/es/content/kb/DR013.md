---
uid: DR013
category: Acciones de código
sub-category: Legibilidad
title: Simplificar la lógica negada
author: Daniel Otykier
updated: 2025-01-09
---

Acción de código `DR013` (Legibilidad) **Simplificar la lógica negada**

## Descripción

Cuando se niega una expresión lógica, a menudo resulta más legible reescribirla usando el operador invertido.

### Ejemplo 1

Cambiar:

```dax
NOT([Sales Amount] = [Budget Amount])
```

A:

```dax
[Sales Amount] <> [Budget Amount]
```

### Ejemplo 2

Cambiar:

```dax
NOT([Cost Amount] < [Sales Amount])
```

A:

```dax
[Cost Amount] >= [Sales Amount]
```

## ¿Por qué Tabular Editor sugiere esto?

La lógica negada puede ser más difícil de leer y entender que la lógica sin negar. Al eliminar la negación (el operador [`NOT`](https://dax.guide/NOT)) e invertir el operador de comparación, el código se vuelve más conciso y más fácil de leer.