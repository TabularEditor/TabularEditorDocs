---
uid: DR012
category: Acciones de código
sub-category: Legibilidad
title: Eliminar BLANK innecesario
author: Daniel Otykier
updated: 2025-01-09
---

Acción de código `DR012` (Legibilidad) **Eliminar BLANK innecesario**

## Descripción

Algunas funciones DAX, como [`IF`](https://dax.guide/IF) y [`SWITCH`](https://dax.guide/SWITCH), ya devuelven [`BLANK()`](https://dax.guide/BLANK) cuando la condición es falsa, por lo que no es necesario especificar `BLANK()` explícitamente.

### Ejemplo

Cambiar:

```dax
SWITCH(
    Document[Type],
    "Invoice", [Invoice Amount],
    "Credit Note", [Credit Note Amount],
    BLANK()
)
```

A:

```dax
SWITCH(
    Document[Type],
    "Invoice", [Invoice Amount],
    "Credit Note", [Credit Note Amount]
)
```

## ¿Por qué Tabular Editor sugiere esto?

La función `BLANK()` es redundante cuando se usa como último argumento en una función `IF` o `SWITCH`, ya que estas funciones ya devuelven `BLANK()` cuando la condición es falsa. Al quitar la función `BLANK()`, el código queda más conciso y es más fácil de leer.